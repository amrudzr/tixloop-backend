Generate Laravel service layer for TixLoop.

Rules:
- business logic inside services
- controllers remain thin
- transaction-safe operations
- reusable logic

Always:
- use dependency injection
- handle exceptions
- return predictable outputs